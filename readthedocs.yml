# .readthedocs.yml

# python:
#   version: 3.8
#   setup_py_install: true

# requirements_file: requirements-doc.txt

version: 2

build:
  tools:
    python: "3.11"

python:
  install:
    - method: setuptools
      path: .
    - requirements: requirements-doc.txt
